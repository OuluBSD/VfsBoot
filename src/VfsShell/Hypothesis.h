#pragma once

#include <functional>
#include <string>
#include <vector>

// Forward declarations for types defined elsewhere
struct Vfs;
struct TagStorage;
struct TagRegistry;
class ContextBuilder;

// Forward declarations for test suite structures
struct ActionPlannerTest;
struct ActionPlannerTestSuite;
struct Hypothesis;
struct HypothesisResult;
struct HypothesisTester;
struct HypothesisTestSuite;
